# Scripts folder

No build step is required for v1 because the workshop ships as a prebuilt static HTML package.

For a future v2, this folder is the place to add:

- SAS-to-profile extraction utilities
- profile validation scripts
- server-backed re-embedding jobs for live UMAP / t-SNE refresh
